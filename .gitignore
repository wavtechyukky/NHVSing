# Logs and checkpoints
logs*/
snapshots*/
*.log

# Dataset files
dataset/
*.npz
*.npy
wav.zip
*_wav_length.txt

# Model outputs
*.onnx
*.pt
*.pth
output*.wav

# V2 exported models — track these explicitly (like exported_kiritan etc.)
!exported_models/
!exported_models/**
# ...but NOT the third-party NSF-HiFiGAN (OpenVPI) used only for the demo's optional comparison
exported_models/nsf_hifigan.onnx

# Demo WAV outputs — track explicitly (reference audio for demo page)
!wav/
!wav/**

# ...but withhold the entire Natsume Singing DB row from Part A (copy-synthesis).
# Redistributing the original recording needs the singer's permission (pending), and the
# V3/V3X/NSF re-synthesis reproduces that same recording — so GT AND re-synthesis are all withheld.
# (Part B DiffSinger voicebank synthesis under audio_v3/synth/ is a separate matter and is kept.)
docs/audio_v3/Natsume_Singing_DB_0713_12_0006_*

# Python
__pycache__/
*.pyc
*.pyo
.ipynb_checkpoints/
*.ipynb
!demo_v2.ipynb
!demo_v3.ipynb

# macOS
.DS_Store

# Notebooks output (keep .ipynb files but ignore checkpoints)
.ipynb_checkpoints/

# Temp / experiment outputs
output_test*/
cepstrum_comparison.png
inference_test/
m4singer_onnx_test*/
paper/
encodec-main/

# Development notes (not for public)
CHANGELOG.md

# Old reference code
20260302/

# Local work folder (handover docs, temp files)
update/
temp/

# VSCode workspace file (local only)
*.code-workspace

# Training result folders (e.g. kiritan_1000epoch/)
*epoch/
